import 'package:flutter/material.dart';

class AppIcons {
  static const Icon airFilter = Icon(Icons.air);
  static const Icon appPreferences = Icon(Icons.settings);
  static const Icon breakFluid = Icon(Icons.pause);
  static const Icon contactUs = Icon(Icons.mail_outline);
  static const Icon coolant = Icon(Icons.device_thermostat);
  static const Icon drivingLog = Icon(Icons.description);
  static const Icon engineOil = Icon(Icons.local_gas_station);
  static const Icon helpCenter = Icon(Icons.help_outline);
  static const Icon home = Icon(Icons.home_filled);
  static const Icon more = Icon(Icons.menu);
  static const Icon navigation = Icon(Icons.near_me);
  static const Icon notifications = Icon(Icons.notifications_none);
  static const Icon privacy = Icon(Icons.shield);
  static const Icon sparkPlugs = Icon(Icons.auto_awesome);
  static const Icon transmissionFluid = Icon(Icons.radio); }
