name: moring
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # <<< 여기를 수정했습니다.

dependencies:
  flutter:
    sdk: flutter
  material_design_icons_flutter: ^7.0.0
  kakao_flutter_sdk: ^1.9.2
  dio: ^5.2.2
  flutter_secure_storage: ^8.0.0
  flutter_mapbox_navigation:
    path: ./flutter_mapbox_navigation
  geolocator: ^10.1.1
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # <<< 여기를 수정했습니다.

flutter:
  uses-material-design: true

  assets:
    - assets/xm3/
    - assets/그렌저/
    - assets/재규어/
dependency_overrides:
  asn1lib: 1.5.8