# Driver Monitoring System Requirements
# Updated for v1ai_model_improved.py and v1ai_model_modified.py

# Core libraries
numpy>=1.20.0
scipy>=1.5.0
opencv-python>=4.5.0

# Ultralytics for YOLOv8 model training and inference
ultralytics>=8.0.0

# MediaPipe for face mesh and landmark detection
mediapipe>=0.10.0

# Face recognition library (uses dlib internally)
face-recognition>=1.3.0

# Machine Learning libraries (for v1ai_model_improved.py)
scikit-learn>=1.0.0

# Pillow for image processing
Pillow>=9.0.0

# Additional dependencies that might be needed
# argparse (built-in Python)
# json (built-in Python)
# os (built-in Python)
# time (built-in Python)
# subprocess (built-in Python)
# sys (built-in Python)
# collections (built-in Python)
# pickle (built-in Python)

# Optional: For development and testing
# jupyter>=1.0.0
# matplotlib>=3.5.0
# seaborn>=0.11.0

# Installation notes:
# 1. dlib (dependency of face-recognition) may require additional system dependencies
# 2. On Windows, you might need Visual Studio Build Tools for dlib
# 3. On Linux, you might need: sudo apt-get install cmake build-essential
# 4. On macOS, you might need: brew install cmake

# For headless systems (no GUI), use:
# opencv-python-headless>=4.5.0