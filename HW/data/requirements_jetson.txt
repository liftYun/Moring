# Jetson Orin Nano/Xavier/Orin 전용 의존성 파일
# ARM 아키텍처에 최적화된 버전 (dlib 19.24.2 지원)

# Core libraries
numpy>=1.20.0
scipy>=1.5.0

# Jetson 전용 OpenCV (CUDA 지원)
# pip install opencv-python 대신 Jetson에 최적화된 버전 사용
# sudo apt-get install python3-opencv

# Ultralytics for YOLOv8 (Jetson 호환)
ultralytics>=8.0.0

# MediaPipe (Jetson 지원)
mediapipe>=0.10.0

# Face recognition (Jetson에서 컴파일 필요)
# face-recognition>=1.3.0

# Machine Learning (Jetson 호환)
scikit-learn>=1.0.0

# Pillow
Pillow>=9.0.0

# Jetson Orin Nano 전용 설치 노트:
# 1. OpenCV: sudo apt-get install python3-opencv
# 2. dlib: Jetson Orin Nano에서 직접 컴파일 필요 (CUDA 8.7 아키텍처)
# 3. CUDA 지원: Jetson에 내장된 CUDA 활용
# 4. TensorRT: 성능 향상을 위해 권장

# 설치 순서:
# 1. sudo apt-get update
# 2. sudo apt-get install python3-opencv
# 3. pip install -r requirements_jetson.txt
# 4. dlib 수동 설치 (아래 참조)

# dlib 설치 명령어 (Jetson Orin Nano용):
# wget http://dlib.net/files/dlib-19.24.2.tar.bz2
# tar -xf dlib-19.24.2.tar.bz2
# cd dlib-19.24.2
# cmake -DCMAKE_BUILD_TYPE=Release -DUSE_AVX_INSTRUCTIONS=0 -DUSE_SSE4_INSTRUCTIONS=OFF -DUSE_SSE2_INSTRUCTIONS=OFF -DUSE_SSE_INSTRUCTIONS=OFF -DUSE_NEON_INSTRUCTIONS=ON -DUSE_BLAS=ON -DUSE_LAPACK=ON -DUSE_CUDA=ON -DCUDA_ARCH_BIN=8.7 -DCUDA_ARCH_PTX=8.7 .
# make -j4
# sudo make install
# sudo python3 setup.py install
